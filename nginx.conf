server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location = /index.html {
    return 301 /;
  }

  location = /services/index.html {
    return 301 /services/;
  }

  location / {
    try_files $uri $uri/ =404;
  }
}
